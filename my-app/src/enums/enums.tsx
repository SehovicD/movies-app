export enum SEARCH_PARAMS {
  OLDER = "older than",
  YOUNGER = "younger than",
  RATING = "rating",
  BEFORE = "before",
  AFTER = "after",
  YEARS = "years",
}

export enum SWITCH_TYPE {
  MOVIES = "movie",
  TV_SHOWS = "tv",
}
